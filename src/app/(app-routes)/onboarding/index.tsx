import OnboardingScreens from "@/screens/onbaording.screens";
import { View, Text } from "react-native";

const Onboarding = () => {
  return (
    <View className="flex-1 bg-secondary">
      <OnboardingScreens />
    </View>
  );
};

export default Onboarding;
