import AppintroScreens from "@/screens/appintro.screens";
import { View } from "react-native";

const Appintro = () => {
  return (
    <View className="flex-1 bg-secondary">
      <AppintroScreens />
    </View>
  );
};

export default Appintro;
